{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "cd backend && python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "cd backend && python manage.py migrate && gunicorn medintel_backend.wsgi:application --bind 0.0.0.0:$PORT"
  }
}