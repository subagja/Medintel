git clone
py -3.12 -m venv venv
pip install -r requirements.txt

venv\Scripts\activate

python manage.py import_signals

1. Aktifkan env crawler:
cd ..\crawler
python crawler.py

2. Aktifkan env dashboard:
cd ..\dashboard
streamlit run app.py

3. Aktifkan env backend:
cd ..\backend
python manage.py runserver

4. import db
cd C:\Users\Admin\tesis\medintel\backend
python manage.py import_signals --raw ..\crawler\output\data_intel_raw.csv

Di Django Admin:

Signals sudah masuk

SignalLocation sudah punya location (banyak yang akan ter-link otomatis)

Locations + Aliases sudah terbentuk dari gazetteer

Ini fondasi penting untuk:
✅ Geocode error center (admin)
✅ Manual correction lokasi
✅ Alert rules (next stage)


//biar venv nya ga error
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
rmdir /s /q venv
py -3.12 -m venv venv
.\venv\Scripts\Activate
python --version
where python
python -m pip install --upgrade pip
pip install -r requirements.txt